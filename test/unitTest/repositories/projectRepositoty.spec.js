const createProjectRepository = require('../../../repositories/projectRepository');

describe('Repository: Project', () => {
    describe('Create method', () => {
        it('Should create project if it not exits', () => {

        })
    });
});
